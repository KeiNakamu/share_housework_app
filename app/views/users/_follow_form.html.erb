<div id="follow_form_<%= user.id %>">
  <%= form_with(model: current_user.active_relationships.build(followed_id: user.id))  do |form| %>
    <%= form.hidden_field :followed_id %>
    <%= form.submit "フォロー" %>
  <% end %>
</div>