<div class="jumbotron text-center hero " style="background:url(<%= asset_path 'file6.jpg' %>); background-size:cover; background-position:center bottom;">
  <div class="container">
    <h2 class="followers mb-3">共有仲間を増やしていこう！</h2>
    <p class="followers">↓ フォロー 一覧はこちら ↓</p>
    <%= link_to "フォロー", followings_user_path(@user), class: "btn btn-primary" %>
  </div>
</div>

<div class="container">
  <div class="row col-md-12 justify-content-center">
    <h3 class="text-center">フォロワー一覧</h3>
  </div>
</div>

<div class="container">
  <div class="row col-md-12 justify-content-center">
    <p><%= @user.followers.count %>フォロー</p>
  </div>
</div>

<div class="container", style="margin-bottom: 300px;">
  <div class="row col-md-12 justify-content-center">
    <table>
      <tbody>
        <div class=" mb-4 text-center" >
          <div class="row no-gutters">
            <div class="col-md-3">
              <% @users.each do |user| %>
                <tr>
                  <td><%= link_to user.name, user, class: "link-to mr-3" %></td>
                  <td><%= render 'follow_form', user: user %></td>
                </tr>
              <% end %>
            </div>
          </div>
        </div>
      </tbody>
    </table>
  </div>
</div>
