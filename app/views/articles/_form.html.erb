<div class="container mt-5 mb-5">
  <div class="row col-md-8 mx-auto">
    <%= form_with(model: @article, local: true ,url: choose_new_or_edit ) do |form| %>
      <% if @article.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(article.errors.count, "つのエラー") %> により投稿ができませんでした！</h2>

          <ul>
            <% article.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="field mb-4">
        <%= form.label :title %>
        <%= form.text_field :title, placeholder: 'タイトルを入力'%>
      </div>

      <div class="field mb-5">
        <%= form.label :image %>
        <%= image_tag(article.image.url) if article.image && article.image.url %>
        <%= form.file_field :image %>
        <%= form.hidden_field :image_cache %>
      </div>

      <div class="field mb-5">
        <h5>・カテゴリを選択してください</h5>
        <%= form.collection_check_boxes(:category_ids, Category.all, :id, :name) do |b| %>
          <%= b.label { b.check_box + b.text } %>
        <% end %>
      </div>

      <p>投稿した内容をシェアする場合は、[公開]を,タスク管理として利用する場合は[非公開]を選択してください</p>

      <div class="field mb-5" id="article_status_id">
          <%= form.select :status, Article.statuses.map { |k, v| [t("article.status.#{k}"), k] }, selected: @article.status %>
      </div>

      <p class="description m-0">非公開を選択時のみ作業日と期限を選択してください</p>
      <p>作業日には、作業する日を入力してください</p>
      <div class="field mb-4">
        <%= form.label :date %>
        <%= form.date_field :date %>
      </div>

        <p>期限には、作業を終わらせたい日を入力してください</p>

      <div class="field">
        <%= form.label :deadline %>
        <%= form.date_field :deadline %>
      </div>

      <%= form.hidden_field :user_id, :value => current_user.id%>

      <h3 class="form-title mt-5 mb-3">手順</h3>
      <p class="description m-0">以下に作業手順を入力してください</p>
      <p>以下の『手順を追加』を押すと入力フォームが追加されます</p>

      <%= form.fields_for :procedures do |f| %>
        <div class="field">
          <%= f.label :'画像' %>
          <%= f.file_field :image %>
          <%= f.hidden_field :image_cache %>
        </div>

        <div class="field">
          <%= f.label :'内容' %>
          <%= f.text_area :content %>
        </div>
      <% end %>

      <div class="add-botton-box my-4">
        <%= link_to_add_association "手順を追加", form, :procedures,
        class: 'add_fields' 'btn btn-info',
        id: 'add-btn',
        data: {
          association_insertion_node: '#detail-association-insertion-point',
          association_insertion_method: 'before'
          }
        %>
      </div>

      <div id="detail-association-insertion-point"></div>

      <div class="actions">
        <%= form.submit "投稿する", class: 'btn btn-primary', style:" margin-bottom: 200px;" %>
      </div>
    <% end %>
  </div>
</div>